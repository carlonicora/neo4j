name: neo4j
services:
  neo4j:
    ports:
      - 7474:7474
      - 7687:7687
    labels:
      # Traefik (optional - uses Coolify's SERVICE_FQDN_NEO4J if set)
      - "traefik.enable=true"
      - "traefik.http.routers.neo4j.rule=Host(`${SERVICE_FQDN_NEO4J:-localhost}`)"
      - "traefik.http.routers.neo4j.entrypoints=https"
      - "traefik.http.routers.neo4j.tls=true"
      - "traefik.http.routers.neo4j.tls.certresolver=letsencrypt"
      - "traefik.http.services.neo4j.loadbalancer.server.port=7474"
    volumes:
      - ./neo4j/data:/data
      - ./neo4j/logs:/logs
      - ./neo4j/import:/var/lib/neo4j/import
      - ./neo4j/plugins:/plugins
    environment:
      - NEO4J_AUTH=${NEO4J_AUTH}
      - NEO4J_PLUGINS=["apoc"]
      - NEO4J_apoc_export_file_enabled=true
      - NEO4J_apoc_import_file_enabled=true
      - NEO4J_dbms_security_procedures_unrestricted=gds.*,apoc.*
      - NEO4J_dbms_security_procedures_allowlist=gds.*,apoc.*
    image: graphstack/dozerdb:5.26.3.0
    restart: always
